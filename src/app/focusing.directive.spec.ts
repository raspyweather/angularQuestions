import { FocusingDirective } from './focusing.directive';

describe('FocusingDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusingDirective();
    expect(directive).toBeTruthy();
  });
});
